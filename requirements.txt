# UploaderDB — versión para otro laboratorio (rama para-otro-lab)
# auto.py solo usa librería estándar. Sin dependencias obligatorias.
# Opcional (solo si reactivas el summary interno, no incluido aquí):
# openpyxl>=3.1.0
requests>=2.28.0
